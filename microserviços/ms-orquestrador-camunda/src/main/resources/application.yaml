server:
  port: 8011
  servlet:
    context-path: /orquestrador-camunda

spring:
  datasource:
    url: jdbc:postgresql://localhost:5437/camunda
    username: dbcamundaadmin
    password: dbcamundapassword

camunda:
  bpm:
    admin-user:
      id: demo
      password: demo

# custom path for swagger-ui
springdoc:
  swagger-ui:
    path: /swaggerui.html
    operationsSorter: alpha
    tagsSorter: alpha
  # custom path for api docs
  api-docs:
    path: /api-docs